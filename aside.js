function pushAsideGpu(x, y){
    $("#gpu-aside").text(`${x} x ${y}`);
}

function pushAsideMot(x){
    $("#mot-aside").text(`${x}`);
}

function pushAsideCpu(x){
    $("#cpu-aside").text(`${x}`);
}

function pushAsideRam(x){
    $("#ram-aside").text(`${x}`);
}
function pushAsideSSD(x){
    $("#ssd-aside").text(`${x}`);
}
function pushAsideCab(x){
    $("#cables-aside").text(`${x}`);
}